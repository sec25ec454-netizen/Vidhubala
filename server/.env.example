# MongoDB Atlas connection string
# URL-encode special characters in the username or password.
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/registration_db?retryWrites=true&w=majority
PORT=5000
